// import React, { lazy } from 'react'
import ComunityReaded from '.';
import RouteProps from '../routes';
const route: RouteProps = {
  path: '/comunity-readed',
  component: <ComunityReaded />,
};

export default route;
