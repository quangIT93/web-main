// import React from 'react'
import Feed from '.';

import CV from '.';
import RouteProps from '../routes';

const route: RouteProps = {
    path: '/cv',
    component: <CV />,
};

export default route;
